# Copy this file to backend/.env and fill the MongoDB connection string.
MONGO_URL=mongodb+srv://<username>:<password>@<cluster-url>/<database>?retryWrites=true&w=majority

# Optional override for public URL used by mobile/QR flow.
# PUBLIC_URL=http://localhost:3000